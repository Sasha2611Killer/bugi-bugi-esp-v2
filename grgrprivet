--[[
    WARNING: Heads up! This script has not been verified by ScriptBlox. Use at your own risk!
]]

local ESP = {}
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- Настройки ESP (изменяйте их в любое время - всё обновится автоматически)
ESP.Settings = {
    Enabled = true,           -- Включить/выключить ESP
    EspBox = true,            -- Показывать бокс
    EspName = true,           -- Показывать имя
    EspHealthBar = true,      -- Показывать полоску здоровья
    EspHealthText = true,     -- Показывать текст здоровья
    BoxColor = Color3.fromRGB(255, 0, 0),        -- Цвет бокса
    NameColor = Color3.fromRGB(255, 255, 255),   -- Цвет имени
    HealthBarWidth = 6,       -- Ширина полоски здоровья
    OutlineColor = Color3.fromRGB(0, 0, 0),      -- Цвет обводки
    Transparency = 1,         -- Прозрачность
    ShowTeammates = false,    -- Показывать тиммейтов
    ShowEnemies = true,       -- Показывать врагов
    ShowSelf = false,         -- Показывать себя
    BoxThickness = 1.6,       -- Толщина бокса
}

-- Внутренние переменные
local Boxes = {}
local Names = {}
local HealthBars = {}
local HealthTexts = {}
local Humanoids = {}
local Connections = {}
local PlayerData = {}

-- Функция для проверки, враг ли игрок
local function IsEnemy(plr)
    if plr == LocalPlayer then 
        return ESP.Settings.ShowSelf
    end

    local myChar = LocalPlayer.Character
    local theirChar = plr.Character
    if not myChar or not theirChar then return false end

    local isEnemy = myChar.Parent.Name ~= theirChar.Parent.Name

    if isEnemy then
        return ESP.Settings.ShowEnemies
    else
        return ESP.Settings.ShowTeammates
    end
end

-- Функция для получения Humanoid
local function GetHumanoid(character)
    if not character then return nil end
    for _, child in pairs(character:GetChildren()) do
        if child:IsA("Humanoid") then
            return child
        end
    end
    return nil
end

-- Очистка данных игрока
local function ClearPlayerData(plr)
    Humanoids[plr] = nil
    PlayerData[plr] = nil

    if Boxes[plr] then
        Boxes[plr].Visible = false
    end
    if Names[plr] then
        Names[plr].Visible = false
    end
    if HealthBars[plr] then
        if HealthBars[plr].bg then
            HealthBars[plr].bg.Visible = false
        end
        if HealthBars[plr].fill then
            HealthBars[plr].fill.Visible = false
        end
    end
    if HealthTexts[plr] then
        HealthTexts[plr].Visible = false
    end
end

-- Обновление Humanoid
local function UpdateHumanoid(plr)
    local char = plr.Character
    if char then
        local newHumanoid = GetHumanoid(char)
        if newHumanoid then
            Humanoids[plr] = newHumanoid
            PlayerData[plr] = {
                lastUpdate = tick(),
                valid = true
            }
            return true
        end
    end

    ClearPlayerData(plr)
    return false
end

-- Очистка соединений
local function ClearConnections(plr)
    if Connections[plr] then
        for _, conn in pairs(Connections[plr]) do
            conn:Disconnect()
        end
        Connections[plr] = nil
    end
end

-- Спрятать весь ESP для игрока
local function HideAllESP(plr)
    if Boxes[plr] then
        Boxes[plr].Visible = false
    end
    if Names[plr] then
        Names[plr].Visible = false
    end
    if HealthBars[plr] then
        if HealthBars[plr].bg then
            HealthBars[plr].bg.Visible = false
        end
        if HealthBars[plr].fill then
            HealthBars[plr].fill.Visible = false
        end
    end
    if HealthTexts[plr] then
        HealthTexts[plr].Visible = false
    end
end

-- Создание ESP для игрока
local function CreateESP(plr)
    if Boxes[plr] then return end

    -- Create box
    local box = Drawing.new("Square")
    box.Thickness = ESP.Settings.BoxThickness
    box.Filled = false
    box.Color = ESP.Settings.BoxColor
    box.Transparency = ESP.Settings.Transparency
    box.Visible = false
    Boxes[plr] = box

    -- Create name tag
    local nameTag = Drawing.new("Text")
    nameTag.Text = plr.Name
    nameTag.Size = 16
    nameTag.Color = ESP.Settings.NameColor
    nameTag.Center = true
    nameTag.Outline = true
    nameTag.OutlineColor = ESP.Settings.OutlineColor
    nameTag.Visible = false
    Names[plr] = nameTag

    -- Create health bar background
    local healthBarBg = Drawing.new("Square")
    healthBarBg.Thickness = 1
    healthBarBg.Filled = true
    healthBarBg.Color = Color3.fromRGB(0, 0, 0)
    healthBarBg.Transparency = 0.7
    healthBarBg.Visible = false
    HealthBars[plr] = {bg = healthBarBg, fill = nil}

    -- Create health bar fill
    local healthBarFill = Drawing.new("Square")
    healthBarFill.Thickness = 1
    healthBarFill.Filled = true
    healthBarFill.Color = Color3.fromRGB(0, 255, 0)
    healthBarFill.Transparency = 0.7
    healthBarFill.Visible = false
    HealthBars[plr].fill = healthBarFill

    -- Create health text
    local healthText = Drawing.new("Text")
    healthText.Size = 14
    healthText.Color = Color3.fromRGB(255, 255, 255)
    healthText.Center = true
    healthText.Outline = true
    healthText.OutlineColor = ESP.Settings.OutlineColor
    healthText.Visible = false
    HealthTexts[plr] = healthText
end

-- Удаление ESP для игрока
local function RemoveESP(plr)
    if Boxes[plr] then
        Boxes[plr]:Remove()
        Boxes[plr] = nil
    end
    if Names[plr] then
        Names[plr]:Remove()
        Names[plr] = nil
    end
    if HealthBars[plr] then
        if HealthBars[plr].bg then
            HealthBars[plr].bg:Remove()
        end
        if HealthBars[plr].fill then
            HealthBars[plr].fill:Remove()
        end
        HealthBars[plr] = nil
    end
    if HealthTexts[plr] then
        HealthTexts[plr]:Remove()
        HealthTexts[plr] = nil
    end

    ClearPlayerData(plr)
    ClearConnections(plr)
end

-- Получение цвета здоровья (УЛУЧШЕНО)
local function GetHealthColor(percent)
    -- Принудительно нормализуем процент
    percent = math.max(0, math.min(1, percent))
    
    if percent > 0.6 then
        return Color3.fromRGB(0, 255, 0) -- Ярко зеленый
    elseif percent > 0.3 then
        return Color3.fromRGB(255, 255, 0) -- Ярко желтый
    elseif percent > 0.1 then
        return Color3.fromRGB(255, 100, 0) -- Оранжевый для критического
    else
        return Color3.fromRGB(255, 0, 0) -- Ярко красный
    end
end

-- Безопасное получение значений здоровья
local function GetSafeHealthValues(humanoid)
    if not humanoid then
        return 0, 100
    end

    local maxHealth = 100
    local success, result = pcall(function()
        return humanoid.MaxHealth
    end)
    if success and result and result > 0 then
        maxHealth = result
    end

    local health = 0
    success, result = pcall(function()
        return humanoid.Health
    end)
    if success and result and result >= 0 then
        health = result
    end

    if health > maxHealth * 2 then
        health = maxHealth
    end

    return health, maxHealth
end

-- Функция для принудительного обновления цвета полоски здоровья (НОВАЯ)
local function UpdateHealthBarColor(plr, healthPercent)
    if not HealthBars[plr] or not HealthBars[plr].fill then return end
    
    local fillBar = HealthBars[plr].fill
    fillBar.Color = GetHealthColor(healthPercent)
    
    -- Принудительно обновляем видимость, чтобы цвет применился
    fillBar.Visible = fillBar.Visible
end

-- Отслеживание смерти и изменений здоровья (УЛУЧШЕНО)
local function TrackDeath(plr, humanoid)
    if not humanoid then return end

    -- Отслеживаем изменения здоровья для обновления цвета
    local healthChangedConn = humanoid:GetPropertyChangedSignal("Health"):Connect(function()
        if Humanoids[plr] == humanoid then
            local health, maxHealth = GetSafeHealthValues(humanoid)
            if maxHealth > 0 then
                local healthPercent = health / maxHealth
                UpdateHealthBarColor(plr, healthPercent)
            end
        end
    end)
    
    if not Connections[plr] then
        Connections[plr] = {}
    end
    table.insert(Connections[plr], healthChangedConn)

    -- Отслеживаем смерть
    local diedConn
    diedConn = humanoid.Died:Connect(function()
        ClearPlayerData(plr)

        task.wait(1)

        if plr.Character then
            UpdateHumanoid(plr)
        end

        if diedConn then
            diedConn:Disconnect()
        end
    end)
    table.insert(Connections[plr], diedConn)
end

-- Обработка добавления персонажа (УЛУЧШЕНО)
local function OnCharacterAdded(plr, char)
    ClearPlayerData(plr)
    ClearConnections(plr)

    Connections[plr] = {}

    task.wait(0.5)

    UpdateHumanoid(plr)

    local humanoid = Humanoids[plr]
    if humanoid then
        TrackDeath(plr, humanoid)
        
        -- Принудительно обновляем цвет при создании персонажа
        local health, maxHealth = GetSafeHealthValues(humanoid)
        if maxHealth > 0 then
            UpdateHealthBarColor(plr, health / maxHealth)
        end
    end

    local childAddedConn = char.ChildAdded:Connect(function(child)
        if child:IsA("Humanoid") then
            UpdateHumanoid(plr)
            TrackDeath(plr, child)
            
            -- Обновляем цвет для нового Humanoid
            local health, maxHealth = GetSafeHealthValues(child)
            if maxHealth > 0 then
                UpdateHealthBarColor(plr, health / maxHealth)
            end
        end
    end)
    table.insert(Connections[plr], childAddedConn)

    local childRemovedConn = char.ChildRemoved:Connect(function(child)
        if child:IsA("Humanoid") then
            ClearPlayerData(plr)
        end
    end)
    table.insert(Connections[plr], childRemovedConn)

    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then
        local hrpChangedConn = hrp:GetPropertyChangedSignal("Parent"):Connect(function()
            if not hrp.Parent then
                ClearPlayerData(plr)
            end
        end)
        table.insert(Connections[plr], hrpChangedConn)
    end

    local charAncestryChangedConn = char.AncestryChanged:Connect(function()
        if not char.Parent then
            ClearPlayerData(plr)
        end
    end)
    table.insert(Connections[plr], charAncestryChangedConn)
end

-- Обработка добавления игрока
local function OnPlayerAdded(plr)
    if plr == LocalPlayer and not ESP.Settings.ShowSelf then 
        return 
    end

    CreateESP(plr)

    local charAddedConn = plr.CharacterAdded:Connect(function(char)
        OnCharacterAdded(plr, char)
    end)

    if not Connections[plr] then
        Connections[plr] = {}
    end
    table.insert(Connections[plr], charAddedConn)

    local charRemovingConn = plr.CharacterRemoving:Connect(function()
        ClearPlayerData(plr)
    end)
    table.insert(Connections[plr], charRemovingConn)

    if plr.Character then
        OnCharacterAdded(plr, plr.Character)
    end
end

-- Отслеживаем изменения в настройках (для автоматического обновления)
local SettingsMetatable = {
    __newindex = function(t, key, value)
        rawset(t, key, value)

        -- Автоматически обновляем соответствующие элементы при изменении настроек
        if key == "BoxColor" or key == "BoxThickness" then
            for _, box in pairs(Boxes) do
                if box then
                    box.Color = ESP.Settings.BoxColor
                    box.Thickness = ESP.Settings.BoxThickness
                end
            end
        elseif key == "NameColor" or key == "OutlineColor" then
            for _, nameTag in pairs(Names) do
                if nameTag then
                    nameTag.Color = ESP.Settings.NameColor
                    nameTag.OutlineColor = ESP.Settings.OutlineColor
                end
            end
            for _, healthText in pairs(HealthTexts) do
                if healthText then
                    healthText.OutlineColor = ESP.Settings.OutlineColor
                end
            end
        end
    end
}

-- Применяем метатаблицу к настройкам
setmetatable(ESP.Settings, SettingsMetatable)

-- Основной цикл обновления (УЛУЧШЕНО)
RunService.RenderStepped:Connect(function()
    for plr, box in pairs(Boxes) do
        -- Проверяем, нужно ли показывать этого игрока
        if plr == LocalPlayer and not ESP.Settings.ShowSelf then
            HideAllESP(plr)
            continue
        end

        -- Если ESP выключен, прячем всё
        if not ESP.Settings.Enabled then
            HideAllESP(plr)
            continue
        end

        local nameTag = Names[plr]
        local healthBar = HealthBars[plr]
        local healthText = HealthTexts[plr]
        local char = plr.Character

        if not char or not char.Parent then
            ClearPlayerData(plr)
            continue
        end

        local hrp = char:FindFirstChild("HumanoidRootPart")
        local head = char:FindFirstChild("Head")

        if not hrp or not head then
            HideAllESP(plr)
            continue
        end

        if not IsEnemy(plr) then
            HideAllESP(plr)
            continue
        end

        local humanoid = Humanoids[plr]

        if not humanoid or not humanoid.Parent then
            if not UpdateHumanoid(plr) then
                HideAllESP(plr)
                continue
            end
            humanoid = Humanoids[plr]
        end

        local health, maxHealth = GetSafeHealthValues(humanoid)

        if health <= 0 or maxHealth <= 0 then
            HideAllESP(plr)
            continue
        end

        local headPos = Camera:WorldToViewportPoint(head.Position + Vector3.new(0, 0.6, 0))
        local rootPos = Camera:WorldToViewportPoint(hrp.Position)
        local legPos = Camera:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3.5, 0))

        if rootPos.Z < 0 then
            HideAllESP(plr)
            continue
        end

        local height = math.abs(headPos.Y - legPos.Y)
        local width = height * 0.55
        local healthPercent = math.max(0, math.min(1, health / maxHealth))

        -- Обновляем box (с текущими настройками)
        if ESP.Settings.EspBox and box then
            box.Size = Vector2.new(width, height)
            box.Position = Vector2.new(rootPos.X - width/2, rootPos.Y - height/2)
            box.Color = ESP.Settings.BoxColor
            box.Thickness = ESP.Settings.BoxThickness
            box.Transparency = ESP.Settings.Transparency
            box.Visible = true
        elseif box then
            box.Visible = false
        end

        -- Обновляем имя (с текущими настройками)
        if ESP.Settings.EspName and nameTag then
            nameTag.Position = Vector2.new(rootPos.X, rootPos.Y - height/2 - 20)
            nameTag.Color = ESP.Settings.NameColor
            nameTag.OutlineColor = ESP.Settings.OutlineColor
            nameTag.Visible = true
        elseif nameTag then
            nameTag.Visible = false
        end

        -- Обновляем полоску здоровья (с текущими настройками)
        if ESP.Settings.EspHealthBar and healthBar and healthBar.bg and healthBar.fill then
            local barWidth = ESP.Settings.HealthBarWidth
            local barHeight = height
            local barX = rootPos.X - width/2 - barWidth - 2
            local barY = rootPos.Y - height/2

            healthBar.bg.Size = Vector2.new(barWidth, barHeight)
            healthBar.bg.Position = Vector2.new(barX, barY)
            healthBar.bg.Visible = true

            local fillHeight = barHeight * healthPercent

            -- Принудительно обновляем цвет каждый кадр для надежности
            healthBar.fill.Color = GetHealthColor(healthPercent)
            fillHeight = math.max(0, math.min(barHeight, fillHeight))

            healthBar.fill.Size = Vector2.new(barWidth, fillHeight)
            healthBar.fill.Position = Vector2.new(barX, barY + (barHeight - fillHeight))
            healthBar.fill.Visible = true
        elseif healthBar then
            if healthBar.bg then healthBar.bg.Visible = false end
            if healthBar.fill then healthBar.fill.Visible = false end
        end

        -- Обновляем текст здоровья (с текущими настройками)
        if ESP.Settings.EspHealthText and healthText and humanoid then
            healthText.Color = GetHealthColor(healthPercent)
            healthText.OutlineColor = ESP.Settings.OutlineColor
            healthText.Text = string.format("%d/%d", math.floor(health), math.floor(maxHealth))
            healthText.Position = Vector2.new(rootPos.X - width/2 - 30, rootPos.Y)
            healthText.Visible = true
        elseif healthText then
            healthText.Visible = false
        end
    end
end)

-- Подключаем события
Players.PlayerAdded:Connect(OnPlayerAdded)
Players.PlayerRemoving:Connect(RemoveESP)

-- Обрабатываем уже существующих игроков
for _, plr in Players:GetPlayers() do
    if plr ~= LocalPlayer or ESP.Settings.ShowSelf then
        OnPlayerAdded(plr)
    end
end

print("ESP loaded with automatic settings update!")

-- Возвращаем таблицу ESP
return ESP
